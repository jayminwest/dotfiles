return {
  {
    -- renders headings, lists, checkboxes, tables, code blocks in the buffer;
    -- the raw markdown shows on the cursor line
    'MeanderingProgrammer/render-markdown.nvim',
    ft = 'markdown',
    opts = {},
  },
}
